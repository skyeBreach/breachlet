# Default Configuration
# This file contains default/fallback config values for the backend

[server]
host = "0.0.0.0"
port = 3000

[database]
address = "0.0.0.0"
port = 5432
database = "breachlet"
user = "postgres"
password = "postgres"

max_connections = 20
min_connections = 5
